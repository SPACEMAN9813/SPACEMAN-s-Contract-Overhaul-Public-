CONTRACT_TYPE
{
    name = KarmanLine
    group = SPACEMAN'sContractOverhaul

    title = The Kármán Line
    agent = Kerbal Rocketry
    sortKey = 200

    synopsis = Reach the Kármán line at 70 km.
    notes = We've proven we can get off the ground. Now we need to ACTUALLY get to space. It's kinda the whole point of a "space" program...
    description = Push higher and reach the edge of space! Launch a rocket and pass 70 km altitude.
    completedMessage = Space! Kerbalkind takes its first real steps to the stars.

    minExpiry = 0
    maxExpiry = 0
    maxCompletions = 1

    cancellable = false
    declinable = false

    targetBody = HomeWorld()

    rewardScience = 1
    rewardReputation = 4
    rewardFunds = 8000

    failureFunds = -2000
    failureReputation = -3

    prestige = Significant

    REQUIREMENT
    {
        name = CompletedOffTheGround
        type = CompleteContract
        contractType = OffTheGround
    }

    PARAMETER
    {
        name = ReachSpace
        type = VesselParameterGroup
        title = Reach the Kármán line
        define = ReachSpace

        PARAMETER
        {
            name = NewVessel
            type = NewVessel
            title = Launch a new vessel
            hideChildren = true
        }

        PARAMETER
        {
            name = Crewmembers
            type = HasCrew
            hideChildren = true
            title = Uncrewed
            minCrew = 0
            maxCrew = 0
        }

        PARAMETER
        {
            name = ReachAltitude
            type = ReachState
            situation = FLYING
            minAltitude = 70000
            disableOnStateChange = true
            title = Reach at least 70 km altitude
            hideChildren = true
        }
    }
}