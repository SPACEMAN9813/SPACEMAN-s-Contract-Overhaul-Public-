CONTRACT_TYPE
{
    name = MinmusSoftLanding
    group = SPACEMAN'sContractOverhaul

    title = Minmus Soft Landing
    agent = Kerbal Rocketry
    sortKey = 640

    synopsis = Perform a controlled, uncrewed soft landing on Minmus.
    notes = We see that Minmus looks like mint ice-cream from orbit, for science, go down to the surface and check. Hopefully it does not melt!
    description = Launch an uncrewed probe and perform a soft landing on Minmus. Touch down intact and transmit data from the surface to complete the mission.
    completedMessage = Success! Your probe has safely landed on Minmus and transmitted surface data.

    minExpiry = 0
    maxExpiry = 0
    maxCompletions = 1

    cancellable = false
    declinable = false

    targetBody = Minmus

    rewardScience = 4
    rewardReputation = 30
    rewardFunds = 90000

    failureFunds = -25000
    failureReputation = -18

    prestige = Significant

    REQUIREMENT
    {
        name = CompletedMinmusOrbit
        type = CompleteContract
        contractType = MinmusOrbiter
    }

    PARAMETER
    {
        name = LandOnMinmus
        type = ReachState
        title = Land on Minmus
        targetBody = Minmus
        situation = LANDED
        disableOnStateChange = true
        hideChildren = true
    }

    PARAMETER
    {
        name = TransmitData
        type = CollectScience
        recoveryMethod = TRANSMIT
        targetBody = Minmus
        situation = SrfLanded
        title = Transmit any science from Minmus' surface
    }
}